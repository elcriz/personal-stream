import React from 'react';
import PropTypes from 'prop-types';

const PeriodNavigation = ({
  //
}) => {
  return (
    <nav className="period-navigation">
      Period Navigation
    </nav>
  );
};

PeriodNavigation.propTypes = {};

export default PeriodNavigation;
